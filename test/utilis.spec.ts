import { expect } from 'chai'
import { BigNumber } from 'ethers'
import { RAY, YEAR_IN_SECONDS } from './constants'
import { calculateCompoundedInterest } from './utils'
describe('Math/Interest Calulation Test', () => {
  it('calculateCompoundedInterest should work', () => {
    //TODO
  })
})
